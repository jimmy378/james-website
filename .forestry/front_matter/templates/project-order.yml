---
label: Project Order
hide_body: true
fields:
- name: projects
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: projects
  label: Projects
pages:
- content/data/order.yaml
