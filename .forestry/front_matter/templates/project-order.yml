---
label: Project Order
hide_body: true
fields:
- name: projects
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: pages
      section: projects
  label: Projects
  description: List of projects based on slug field (must match)
